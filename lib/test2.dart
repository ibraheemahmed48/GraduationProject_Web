// import 'dart:html';// import 'package:flutter/material.dart';//////////// class HomePage extends StatefulWidget {//   @override//   _HomePageState createState() => _HomePageState();// }//// class _HomePageState extends State<HomePage> {//   String? _fileName;//   String? _status;////   // Function to upload the Excel file to Firestore Database//   Future<void> _uploadExcelFileToFirestore() async {//     // Get the uploaded file from the input element//     final inputFile = (querySelector('#inputFile') as FileUploadInputElement).files.first;////     setState(() {//       _status = 'Reading file...';//     });////     // Read the Excel file using the xlsx package//     final bytes = await inputFile.readAsBytes();//     final excel = xlsx.decodeBytes(bytes);////     setState(() {//       _status = 'Uploading data to Firestore...';//     });////     // Get the first worksheet in the Excel file//     final sheet = excel['Sheet1'];////     // Loop through the rows in the worksheet//     for (var row in sheet.rows) {//       // Create a Firestore document for each row//       final data = {//         'name': row[0].value,//         'age': row[1].value.toInt(),//         'email': row[2].value//       };////       // Upload the data to Firestore//       await firebase.firestore().collection('users').add(data);//     }////     setState(() {//       _status = 'File uploaded successfully!';//     });//   }////   @override//   Widget build(BuildContext context) {//     return Scaffold(//       appBar: AppBar(//         title: Text('Flutter Excel to Firestore'),//       ),//       body: Padding(//         padding: const EdgeInsets.all(16.0),//         child: Column(//           crossAxisAlignment: CrossAxisAlignment.start,//           children: [//             Text(//               'Upload Excel File to Firestore',//               style: TextStyle(fontSize: 20.0, fontWeight: FontWeight.bold),//             ),//             SizedBox(height: 16.0),//             Text('Select an Excel file (.xlsx)'),//             SizedBox(height: 8.0),//             ElevatedButton(//               onPressed: () {//                 // Show the file picker dialog//                 final input = FileUploadInputElement()..accept = '.xlsx';//                 input.click();////                 // Get the selected file name and display it//                 input.onChange.listen((event) {//                   setState(() {//                     _fileName = input.files.first.name;//                   });//                 });//               },//               child: Text('Select File'),//             ),//             SizedBox(height: 16.0),//             if (_fileName != null) Text('Selected File: $_fileName'),//             SizedBox(height: 16.0),//             ElevatedButton(//               onPressed: _fileName != null ? _uploadExcelFileToFirestore : null,//               child: Text('Upload to Firestore'),//             ),//             SizedBox(height: 16.0),//             if (_status != null) Text(_status),//           ],//         ),//       ),//     );//   }// }